import * as React from 'react'

import { Theme } from './components/Theme'
import { App } from './screens'

export const Main = () => {
    return (
        <Theme>
            <App />
        </Theme>
    )
}
